# =============================================================================
# Dependencias del DASHBOARD + pipeline batch. Es lo que instala Streamlit
# Community Cloud (build liviano y confiable).
#
# NO incluye orquestación (prefect) ni streaming (confluent-kafka): esas viven
# en requirements-orchestration.txt y requirements-streaming.txt.
#
# Versiones fijadas para resolver limpio en Python 3.14 (Streamlit Cloud):
# streamlit 1.60 acepta pandas 2.3.x; todas tienen wheels para cp314.
#
# Instalar:  python -m pip install -r requirements.txt
# =============================================================================

# Procesamiento y data lake
pandas==2.3.3
deltalake==1.6.2
pyarrow==24.0.0
numpy==2.5.1
requests==2.32.5
python-dotenv==1.2.2

# Calidad de datos / contratos entre capas
pandera==0.32.1

# Dashboard / visualización
streamlit==1.60.0
plotly==6.9.0
