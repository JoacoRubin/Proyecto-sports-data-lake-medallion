# =============================================================================
# Servicio de inferencia (services/inference_api). Separado del dashboard:
# el deploy de la API no necesita streamlit/plotly, y el de Streamlit Cloud
# no necesita un servidor ASGI. Por eso depende de requirements-core.txt, no
# de requirements.txt -- ese ultimo SI incluye streamlit/plotly.
#
# Instalar:  python -m pip install -r requirements-api.txt
# Correr:    uvicorn services.inference_api.main:app --reload
# =============================================================================

-r requirements-core.txt

fastapi==0.139.2
uvicorn==0.51.0
