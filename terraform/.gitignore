# Local state: contiene ARNs y podria contener valores sensibles en algun
# recurso futuro. No hay backend remoto (ver versions.tf) -- este archivo
# vive solo en la maquina que opera la infra.
*.tfstate
*.tfstate.*
.terraform/
crash.log
crash.*.log
*.tfvars
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraform.tfstate.lock.info
